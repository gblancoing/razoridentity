@{
    Layout = "/Pages/Shared/_Layout.cshtml";
}
<div class="flex min-h-screen bg-slate-50">
    <aside class="fixed left-0 top-0 z-20 h-screen w-72 bg-slate-100 border-r border-slate-200 flex flex-col" aria-label="Menú de cuenta">
        <div class="p-4 flex items-center gap-3 border-b border-slate-200">
            <a asp-page="./Index" class="flex items-center gap-3 min-w-0">
                <div class="w-10 h-10 bg-ritweb-teal rounded-lg flex items-center justify-center text-white font-bold text-xl flex-shrink-0">R</div>
                <div class="min-w-0">
                    <h1 class="font-bold text-lg leading-tight tracking-tight text-slate-800 truncate">RitWeb</h1>
                    <p class="text-[10px] uppercase tracking-widest text-slate-500 font-semibold">Mi cuenta</p>
                </div>
            </a>
        </div>
        <partial name="_ManageNav" />
    </aside>
    <main class="flex-1 ml-72 p-8 lg:p-12 min-h-screen">
        @RenderBody()
    </main>
</div>
@await RenderSectionAsync("Scripts", required: false)
