@* Ruta legada: el hub de exploración vive en /categorias (Todas las categorías). *@
@page "/buyer"
@inject NavigationManager Navigation

<PageTitle>ComunaClic</PageTitle>

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/categorias", replace: true);
    }
}
