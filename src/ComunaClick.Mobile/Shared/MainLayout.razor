@inherits LayoutComponentBase
@inject ComunaClick.SharedUI.Services.AuthStateService AuthState
@implements IDisposable

<!-- Layout columna flex: header estático + scroll en contenido -->
<!-- No usa position:fixed que falla en MAUI Android WebView con edge-to-edge -->
<div style="display:flex;flex-direction:column;height:100%;overflow:hidden">

    <!-- Barra superior: estática, no se desplaza con el scroll -->
    <div style="flex-shrink:0;
                background:#ffffff;
                border-bottom:1px solid #e5e7eb;
                box-shadow:0 2px 8px rgba(0,0,0,0.08);
                padding-top:env(safe-area-inset-top,0px)">
        <div style="display:flex;align-items:center;justify-content:space-between;
                    height:56px;padding:0 16px">

            <!-- Logo -->
            <a href="/" style="display:flex;align-items:center;text-decoration:none">
                <img src="_content/ComunaClick.SharedUI/logo.png"
                     alt="ComunaClic"
                     style="height:34px;width:auto" />
            </a>

            <!-- Acciones derecha -->
            <div style="display:flex;align-items:center;gap:10px">

                <!-- Carrito -->
                <a href="/buyer/cart"
                   style="display:flex;align-items:center;justify-content:center;
                          width:40px;height:40px;border-radius:50%;
                          text-decoration:none;color:#2c2f30">
                    <span class="material-symbols-outlined" style="font-size:22px">shopping_cart</span>
                </a>

                @if (!_isAuthenticated)
                {
                    <a href="/login"
                       style="background:#3d6a00;color:#ffffff;
                              padding:8px 16px;border-radius:999px;
                              font-size:13px;font-weight:700;
                              text-decoration:none;white-space:nowrap">
                        Iniciar sesión
                    </a>
                }
                else
                {
                    <a href="/account/buyer"
                       style="display:flex;align-items:center;justify-content:center;
                              width:40px;height:40px;border-radius:50%;
                              background:#f3f4f6;text-decoration:none;color:#2c2f30">
                        <span class="material-symbols-outlined" style="font-size:22px">person</span>
                    </a>
                }
            </div>
        </div>
    </div>

    <!-- Área de contenido: hace scroll -->
    <div style="flex:1;overflow-y:auto;overflow-x:hidden;-webkit-overflow-scrolling:touch">
        @Body
    </div>

</div>

@code {
    private bool _isAuthenticated;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (!firstRender) return;
        try
        {
            await AuthState.InitializeAsync();
            _isAuthenticated = AuthState.IsAuthenticated;
            await InvokeAsync(StateHasChanged);
        }
        catch { }
    }

    void IDisposable.Dispose() { }
}
